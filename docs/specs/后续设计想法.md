CodingAgent(战略转向)

不再按「复刻 Claude Code」的路线继续,而是转向项目真正的价值锚点:
> **一个能理解复杂项目的 CodingAgent** —— 启动陌生代码库时自动建立代码智能引擎(codebase-memory-mcp
> 知识图谱),通过 ponytail 的「懒人资深工程师」阶梯 + 图谱的影响面分析,强制 agent 用**最小 diff, 最快速度** 完成任务。
>
> 本文是转向的权威地图。现有阶段是坚固底座(权限/审计/子代理/技能/MCP/上下文工程),
> 本阶段在其上长出差异化能力层。

## 0. 战略背景与价值定位

我们把 23f77fbe 提交(merge: feat/15-mcp (阶段 15 MCP 客户端)) 做为上个版本的结尾

### 0.1 为什么转向

阶段 01-19 逐步复刻了 Claude Code 的完整 harness(配置/模型/工具/权限/引擎/CLI/上下文/钩子/
压缩/任务/会话/子代理/技能/MCP),质量扎实、测试全绿。但**独立使用价值低**——Claude Code 本身
免费且成熟,「另一个复刻」没有差异化意义。

真正的价值锚点(用户重新定义):
1. **代码库智能引擎自动启动**:陌生库 → 自动索引 → 知识图谱可用,agent 不靠反复 grep/read 摸结构
2. **最小改动执行**:基于图谱影响面分析,让 agent 用最小 diff 完成任务(而非大改)
3. **ponytail 融合**:借 ponytail 的「懒人资深工程师」阶梯,把最小改动从「提示词要求」变成「引擎行为」

差异化点 = Claude Code 的通用定位没覆盖的**垂直场景**:针对复杂项目、以最小代码改动为目标。

目前实现: 提交 7a1df5f6 。 代码内容太粗糙了。 而且loop.py 放置的代码位置完全不对。 我们是应该在写操作前去拦截（最好的代码是从未写过的代码）， 而不是执行完成后去补救
你需要重新阅读E:\Mac\CodeSage\ponytail项目, 由于我们是仿照Claude， 使用可以看ponytail/.claude-plugin, E:\Mac\CodeSage\ponytail\hooks, Readme等文件

### 0.2 探索 DeepSeek Harness KVCache 实现 (为什么他们的缓存这么高) 
1. 项目源码 E:\Mac\CodeSage\deepseek-harness
2. 探索 DeepSeek Harness KVCache 实现 (为什么他们的缓存这么高)
3. 我们需要观测手段， 去探索DeepSeek Harness的UI显示， 缓存利用率是怎么计算得到的，怎么显示出来的

结论:是 API 侧前缀缓存(provider prefix caching),不是 harness 本地缓存。
公式(deepseek-harness/packages/client/ui-conversation/src/client/chat/StatsLine.tsx:109-114):
缓存利用率 = cacheReadTokens / (uncachedInputTokens + cacheReadTokens + cacheWriteTokens) × 100%
- 分子 = 缓存命中 token;分母 = 全部计费输入 token(不含 output——输出从不被缓存承接,含它会无意义拉低比率;该决策记录在归档 note 2026-07-21-tui-footer-cache-hit-rate.zh.md,备选方案被否决的原因都在)
- 分母为 0 时返回 null,UI 整段隐藏(不显示 0%)
显示:每条助手消息底部 Cache hit {percent}%(StatsLine 注册于 ui-conversation/src/client/apply.ts:430 的 conversation.composer.dock slot;中文文案 '缓存命中 {percent}%' 在 locales.ts:63)。
数据流:DeepSeek API 返回 prompt_cache_hit_tokens/prompt_tokens_details.cached_tokens(llm-deepseek/src/types.ts:158-162)→ translate.ts:53-62 的 mapUsage 映射(inputTokens = prompt_tokens − cacheRead,桶互不重叠)→ token-meter
对带 usage 的 assistant/message 事件求和(绝不用 chunk,防重复计数)→ useProjection('tokenUsage') → UI。
为什么他们缓存高:harness 侧的工程是稳定请求前缀——system prompt、工具目录跨轮保持不变(agent-presets 配置里写明 "The tool catalog stays the same across rounds for request-cache stability"),让 API 侧 prefix cache 命中。
对我们 CodeSage 的启示:数据已具备——ai/adapters/openai_compatible.py:248 已把 prompt_tokens_details.cached_tokens 归一化进 usage。可直接照抄公式,在 CLI statusbar/render 显示同样的缓存命中率;并检查我们的 system prompt
是否跨轮稳定(工具描述等)。


### 0.3 系统功能需求判断 (下面的内容哪些可以做， 哪些不用做)
1.  Plan 模式
下面是ClaudeCode的Plan模式介绍: 
先想清楚再动手——Plan 模式是 Claude Code 中唯一一个主动降低自身权限来换取用户信任的机制。
为什么需要 Plan 模式
想象这样一个场景：你让 Claude Code "重构整个认证系统"。它二话不说就开始改文件——改了 12 个文件、删了 3 个函数、引入了一个你完全不想要的 JWT 库。你只能 git checkout . 然后重来。
这就是没有 Plan 模式的世界。
Plan 模式的设计理念是：对于复杂任务，让模型先探索、再规划，用户审批后才动手。它通过权限降级（禁止一切写操作）强制模型进入"只读探索 + 输出计划"的工作模式，直到用户明确批准计划后才恢复写权限。
ClaudeCode关键文件：
文件	行数	职责
src/tools/EnterPlanModeTool/EnterPlanModeTool.ts	127 行	进入 Plan 模式的工具
src/tools/ExitPlanModeTool/ExitPlanModeV2Tool.ts	493 行	退出 Plan 模式 + 审批流程
src/utils/plans.ts	398 行	计划文件管理（slug、读写、恢复）
src/utils/planModeV2.ts	96 行	配置（Agent 数量、实验变体）
src/utils/messages.ts:3136-3417	~280 行	Plan 模式系统消息生成
src/utils/permissions/permissionSetup.ts	~60 行	权限上下文准备与恢复
src/bootstrap/state.ts:1349-1470	~120 行	Plan 模式全局状态
2. 代码编辑策略
会写代码只是起点，好用的 coding agent 还得会用低破坏性的方式改代码。
代码编辑是 coding agent 最关键也最危险的能力——一次错误的编辑可能破坏整个代码库，而一套好的编辑策略能让 agent 像经验丰富的开发者一样精准地改代码。Claude Code 的编辑策略围绕三个原则设计：最小化破坏性，只改需要改的部分；可验证性，每次编辑都有明确的 before/after；抗幻觉，模型无法静默写入不存在的代码。search-and-replace 式的 FileEditTool 天然满足这三个约束，所以 Claude Code 优先使用它；全文件覆盖的 FileWriteTool 只留给创建新文件或需要完整重写的场景。
Claude Code 提供两种文件编辑工具，各有其适用场景：
工具	策略	适用场景	破坏性
FileEditTool	search-and-replace	修改已有文件中的特定部分	低
FileWriteTool	全文件覆盖写入	创建新文件或完整重写	高
系统提示词与工具描述共同引导模型优先使用 FileEditTool——工具描述里更是写死了"只在创建新文件或完整重写时才用 Write"。
FileEditTool 之所以成为默认选择，是因为它在工程上解决了 LLM 编辑代码最棘手的问题：如何让一个可能产生幻觉的模型安全地修改真实代码
3. 系统提示词设计
Claude Code 所有系统提示词的速查参考。每个提示词提供英文原文，点击"中文翻译"可展开查看翻译。
关键源码入口：src/constants/prompts.ts（~915 行）
概览
Claude Code 的系统提示词由两部分组成：7 个静态 section 全局缓存，多个动态 section 每轮或按需计算。
Section	函数名	用途
Intro	getSimpleIntroSection()	身份定义 + 安全边界
System	getSimpleSystemSection()	运行环境规则
Doing Tasks	getSimpleDoingTasksSection()	编码原则与行为规范
Actions	getActionsSection()	风险评估框架
Using Your Tools	getUsingYourToolsSection()	工具使用指南
Tone and Style	getSimpleToneAndStyleSection()	语气与格式
Output Efficiency	getOutputEfficiencySection()	输出效率
4. 用户体验设计
好用 = 模型能力 x 交互设计 x 工程约束
Claude Code 终端 UI 的完整技术栈，围绕三层核心展开：
渲染引擎：自研 Ink/React 终端渲染器，包括 Yoga 布局、Screen Buffer diff、对象池内存优化
数据流：从 API SSE 到终端渲染的 Stream<T> + async function* 流式管线，以及 StreamingMarkdown 增量解析
交互层：工具调用透明度、错误自动恢复、键盘快捷键、Vim 模式，以及带虚拟滚动、权限防误触、会话恢复的 REPL 主界面
此外还涵盖终端协议支持、诊断界面、成本追踪、搜索与文本选择四部分
5. 自治与续跑——/goal 与 /loop
 /goal、/loop、dynamic workflow、auto mode 这些能力全在快照之后，没有源码可看。
/goal 和 /loop 怎么让 Claude 自己接着干
/goal 是“盯着一个条件、不达成不罢休”。你给它一句完成条件，它就一轮一轮干下去，每轮结束由一个独立的裁判判一次“达成没有”，没达成就带着裁判给的理由再来一轮，达成了才停。它是被动的：什么时候停，交给裁判说了算。
/loop 是“定个闹钟、反复来”。你给它一个间隔（或者让它自己定节奏），它就按点反复跑同一件事。它是主动的：什么时候再来，由调度决定，跟“有没有干成”无关。
一个靠“守门人”决定何时收手，一个靠“闹钟”决定何时再来——抓住这条区别，也就抓住了 Claude Code 自治的两条主线。下面分别拆。
6. Auto Mode——权限进入分类器时代
 是"按规则匹配，碰到危险动作就弹个确认框问你"。
但让 Claude 盯着一个目标连跑几十轮、无人值守——跟"每个动作都停下来问人"是矛盾的：你不可能守在屏幕前一遍遍点"同意"。
Auto Mode 是它的解法：一个机器学习分类器，读着当前上下文，逐个动作实时判"这个能自动放行，还是得停下来问人"，而且能听懂你在 CLAUDE.md 或对话里随口说的"别 push"。
这一章的证据分两头：机制（怎么装配、两段式怎么跑、看得到什么）全在 2026 年 3 月底那份 v2.1.88 快照源码里，可逐行读；
分类器读的那套规则正文打包成 bundled 文件、没随快照泄露，但当前 2.1.201 版本用一条命令 claude auto-mode defaults 就能自己吐出来。两头拼起来，这一章能落到源码行号。
7. Agent Teams——对等组队与跨会话安全
多 Agent——主 Agent 派子 Agent、协调器分派 worker、Swarm 铺开一批 worker——形态各异，骨子里是同一种关系：
有一个主体在上面决定派谁、何时收，被派出去的 subagent 是叶子，干完把结果交回就退场
另一种关系：一个 team 里仍有一个固定的 lead 会话在牵头，但除它之外的成员不再是干完就把结果回传、彼此不通气的叶子——他们能用一个叫 SendMessage 的工具互相直接说话、共享一份任务列表和一份团队记忆，成员甚至可能分属不同会话、不同机器。这就是 Agent Teams。一旦消息能在平级成员、乃至不同会话之间横向流动

┌─────┬────────────────┬────────────────────┬────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
│  #  │      功能      │        裁决        │                                                                                      理由                                                                                      │
├─────┼────────────────┼────────────────────┼────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ 1   │ Plan 模式      │ ✅  做(优先)        │ 直接服务「低破坏性/最小改动」价值锚点;底座已有 deny>ask>allow 权限链,「只读模式」可零成本表达(写工具全 deny + 计划文件管理 + EnterPlanMode/ExitPlanMode                        │
│     │                │                    │ 工具)。是用户信任感最强的功能,且我们已有比 Claude Code 更深的引擎强制手段                                                                                                      │
├─────┼────────────────┼────────────────────┼────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ 2   │ 代码编辑策略   │ ✅  做(小 diff)     │ 工具层已完成(Edit=search-and-replace 低破坏、Write=全文件覆盖);缺的是系统提示词引导「优先 Edit,只在新建/完整重写用 Write」+ 工具描述硬化。与 spec 20 约束层天然合流            │
├─────┼────────────────┼────────────────────┼────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ 3   │ 系统提示词设计 │ ✅  做(轻量)        │ cli/base_prompt.py 已有;拆成静态 section + 动态 section 是组织性改动,ponytail 阶梯常驻提示本来就规划在 spec 20 §1.1,顺手完成。影响所有其他功能                                 │
├─────┼────────────────┼────────────────────┼────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ 4   │ 用户体验设计   │ ❌  不做            │ spec 20 已裁;自研 Ink/React 渲染器是大工程且无差异化(Claude Code 做得更好)。只做增量:statusbar 加缓存命中率(0.2 的结论直接可用)                                                │
├─────┼────────────────┼────────────────────┼────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ 5   │ /goal 与 /loop │ ⚠️ 部分做(21       │ /loop = 定时调度,引擎循环上加个小调度器即可,便宜;/goal = 裁判判完成条件的循环,是「自治 CodingAgent」的真实差异化,但需要裁判提示词 + 会话恢复,放阶段                            │
│     │                │ 阶段候选)          │ 21。两者都不服务最小改动锚点,不值得挤占 20                                                                                                                                     │
├─────┼────────────────┼────────────────────┼────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ 6   │ Auto Mode      │ ❌  不做            │ ML 分类器判权限:重(需训练 + 运行时),且与核心不变量「每次决策恰好一条审计事件」冲突(黑盒分类器破坏可解释性/审计);DeepSeek/OpenAI adapter 也没有现成分类器。规则版近似 =         │
│     │                │                    │ 只是权限配置,现有权限引擎已覆盖                                                                                                                                                │
├─────┼────────────────┼────────────────────┼────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ 7   │ Agent Teams    │ ❌  不做(现阶段)    │ 对等组队 = SendMessage 横向协议 + 共享任务表 + 跨会话身份,是大面积工程,不服务「最小改动」锚点;子代理(13)已覆盖层级委派。20 之后再说                                            │
└─────┴────────────────┴────────────────────┴────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘











阶段 20 修正:最小改动 CodingAgent(intel 三文件重写 + 接线修正)

Context

docs/specs/后续设计想法.md 判定当前阶段 20 实现(提交 7a1df5f6)不达生产需求:

1. 位置错:engine/loop.py:569-574 在工具执行后才给建议(_apply_minimal_change_advice L954-979 prepend),spec 20 §4.1 要求执行前拦截——「最好的代码是从未写过的代码」
2. intel/ponytail.py:与生产级 ponytail 差距极大——只有 1 个技能、正文是节选;生产版是 7 组件(完整正文/模式状态机/注入钩子/模式过滤/命令/子代理传染/状态栏)
3. intel/minimal_change.py:无健壮性(每次写操作同步 subprocess、无缓存/超时分级)、「拦截」名存实亡(只返回字符串)、建议只数 callers_total 一维、ambiguous 歧义被误判「无调用者」
4. intel/service.py:CLI 参数格式用错、文本表格解析丢嵌套、format:"json" 结构化输出未用、死字段死方法、CODESAGE_NO_INTEL 未实现

已确认决策(用户):拦截语义 = 同一目标拦一次、重试放行;ponytail 技能正文 = 中文全文翻译。

已实测确认的事实(设计依据)

codebase-memory-mcp(本机已装,实测)
- CLI:codebase-memory-mcp cli [--progress] [--json] <tool_name> <json_args>,参数是 JSON 对象(非 --flag);本库已索引,project key=E-Mac-CodeSage-codesage
- trace_path 带 "format":"json" → 返回 {content, structuredContent, isError},structuredContent 为真结构化 JSON(callers_total/callers: {cols, groups})
- trace_path 短名 → {"status":"ambiguous","suggestions":[{"qualified_name","file_path",...}]}(现有代码误判为 0 调用者)
- list_projects → 原生 JSON;get_architecture/detect_changes/query_graph → 文本表格(无 structuredContent)

引擎接线(已读原文)
- tool_queue.py:ToolUseQueue._execute 序 = abort → pre_hook(L168)→ permission_check(L175)→ tool.call(L196);L134-141 sibling 策略豁免 permission_blocked
- loop.py:_execute_tools 组装队列 L942-951;AgentLoopConfig.intel L121-123;_permission_check L1026
- cli/assemble.py L69-80 同步 _asyncio.run(ensure_indexed()) 阻塞启动 300s;cli/__init__.py L211 enable_intel=True
- MCP 客户端(mcp/client.py McpManager)存在,但 get_builtin_mcp_configs() 只从 installed.json 合成条目,默认机器上 codebase-memory 未连接 → subprocess 为唯一传输,预留 seam

ponytail 生产规格(已深挖):①完整 SKILL.md ②模式状态机(off/lite/full/ultra,flag 文件,默认 PONYTAIL_DEFAULT_MODE>config>full,停用短语整条匹配)③注入钩子(SessionStart/UserPromptSubmit/SubagentStart)④按模式过滤正文(只删 Intensity
表行+带引号示例行,统一头 PONYTAIL MODE ACTIVE — level: X)⑤6 命令 ⑥6 技能 ⑦状态栏。现有只有①且不完整。

方案

1. intel/service.py 重写

- 传输 seam:class ToolCaller(Protocol): async def call(tool, args, *, timeout_s) -> dict;CodeIntelligenceService(project_dir, cbm_cli=None, caller=None) 默认 _CliCaller(测试注入 FakeCaller)
- _CliCaller:修正命令为 [cbm, "cli", "--json", tool, json.dumps(args)];解析顺序:整体 json.loads → 逐行找最后一行 JSON → 纯文本 → CallerError("protocol");CallerError.tier: not_installed|timeout|protocol|server_error|not_found
- 统一解包 parse_tool_result(raw):structuredContent 优先 → content[0].text 走 _parse_table_text(升级 KV 解析:数字/布尔 coercion,括号内容保留在值里)→ 原样透传;isError → CallerError("server_error")
- impact_of_change 归一化契约:{target, kind: symbol|file, status: ok|ambiguous|not_found|error, callers_total, callers: list[str]前20, suggestions};symbol 路径:直接 trace_path format=json,ambiguous → 透传 suggestions;file
  路径:取 Path(path).stem 查 → ambiguous 时按 suggestions[].file_path 过滤命中 → 对前 3 个 qualified 逐个 trace 聚合(有界 ≤4 次调用,ponytail: 注释天花板)
- 健壮性:查询超时 15s/索引 300s;trace/search/list_projects 超时重试 1 次,index_repository 不重试;错误按 (tool, tier) 限频日志;任何失败 → 返回 None 不抛;_impact_cache LRU 128 + TTL 30s;asyncio.Lock 包
  ensure_indexed;start_background_index() daemon 线程(不阻塞启动)+ wait_ready(timeout_s=10) 供 guard
- 删除:死字段 _last_error(改错误分级计数)、_parse_kv_text(被 _parse_table_text 取代)、_run_cli(并入 _CliCaller);重写 changed_symbols(detect_changes 文本表格尽力解析)

2. intel/minimal_change.py 重写

- 核心语义:返回 ToolResult | None 的执行前拦截(不再返回字符串)。WRITE_TOOLS = {"Write","Edit"} 保留;新增 MINIMAL_CHANGE_BLOCKED = "minimal_change_blocked" error_code(不污染 permission_blocked)
- MinimalChangeGuard 每会话一个实例(loop 持有):_seen: dict[normalized_target, int];拦一次,重试放行(用户已确认)——_seen[tkey] >= 1 → None;拦截时 _seen[tkey] = 1
- fail-open 顺序:非写工具 → 开关关(CODESAGE_NO_MINIMAL_CHANGE 保留)→ 无 target → intel 不可用 → wait_ready() 超时 → impact 查询异常 → 全部放行
- 多场景建议 build_advice(target, impact)(ponytail 阶梯编码 + 输出契约):
 - ambiguous:列前 5 个 qualified_name + file_path,指示用全限定名重试或选候选
 - ok + 0 调用者:阶梯 1 YAGNI——先确认是否真的需要,新逻辑查库内 helper 复用
 - ok + 1 调用者:阶梯 2 根因——改共享函数优先于逐调用点
 - ok + N 调用者:阶梯 2/6——影响 N 个调用者(列前 3),改共享函数/根因一处,一行能解决则一行
 - not_found:新文件/未索引——YAGNI 提示
 - 统一格式:[minimal-change] 改动 {target}:{场景}... + 若该改动确为有意为之,直接重试该操作将放行。 + [code] — skipped: [X], add when [Y].
- 删除:模块级 minimal_change_guard(item, intel, state)(state 未用、每次 new 实例)、ladder_suggestion(一维计数,被 build_advice 取代);minimal_change.py 不再 import ponytail(解耦)

3. intel/ponytail.py 重写

- ①完整正文:PONYTAIL_FULL_BODY = 参考 ponytail/skills/ponytail/SKILL.md(120 行)逐节中文全文翻译:7 档阶梯 + 运行时机(理解问题之后)+ Bug 修复=根因 + Rules 全 7 条(含 ponytail: 注释约定)+ Output 契约([code] → skipped: [X], add
  when [Y].)+ Intensity 表(lite/full/ultra + 示例)+ When NOT to be lazy + Boundaries + Persistence
- ②模式状态机:PonytailState(mode: off|lite|full|ultra,默认 full);load_mode(cwd):PONYTAIL_DEFAULT_MODE env > 进程内 set > {cwd}/.ponytail-active flag 文件 > "full";set_mode(off 删 flag,其他写 flag);is_off_phrase(message)
  整条匹配 {"stop ponytail","normal mode"}(纯函数)
- ④按模式过滤:ponytail_body_for(mode):off→"";lite/full/ultra→_filter_body(删 Intensity 表中非当前模式的行 + 删带引号 worked example 行,其余全文保留)+ 头 PONYTAIL MODE ACTIVE — level: {mode};异常回退压缩版常量
  _COMPRESSED_BODY;模块级 memoize
- ⑤⑥6 技能 + 命令:register_ponytail() 注册全部 6 个(ponytail/ponytail-review/ponytail-audit/ponytail-debt/ponytail-gain/ponytail-help),各自 body = 对应 SKILL.md 中文翻译(contract 原样:review 的 L<line>: <tag>、gain
  禁编造数字、debt 台账格式);幂等(模块级 _registered: set[str]);命令 /ponytail lite|full|ultra|off + /ponytail-help 挂 cli/commands.py(参照现有 _cmd_mode 模式);其余 4 个技能走 repl.py 现成的 _invoke_skill_fallback(不用改
  commands.py)
- 注入落位(不做 hook 事件,等价落位):主会话 system prompt 注入(cli/__init__.py L196-198 register_ponytail 后);子代理传染(agents/runner.py 子代理 system prompt 后 append,off 不注入,异常回退压缩版);停用短语拦截(cli/repl.py
  用户输入提交处 2 行:is_off_phrase(line) → set_mode("off"))
- 主会话 mid-session 切换局限(ponytail: 注释):主 prompt 在 build_loop 时固定,mid-session 切换只影响子代理/下次会话

4. 接线修正

engine/tool_queue.py
1. 构造器加 minimal_change_check: Any | None = None(L77-88)+ self._minimal_change_check
2. L175-181 权限闸之后、L185 on_tool_start 之前插入:blocked = await self._minimal_change_check(item); if blocked: blocked.metadata.setdefault("error_code","minimal_change_blocked"); post_hook; return blocked
3. L134-141 sibling 豁免:!= "permission_blocked" → not in _NON_FATAL_CODES(模块常量 {"permission_blocked","minimal_change_blocked"})

engine/loop.py
1. 删 L569-574 调用块 + L954-979 _apply_minimal_change_advice 整个方法(工具名硬编码随删,WRITE_TOOLS 单源化)
2. L942-951 队列组装加 minimal_change_check=self._minimal_change_check
3. 新增 _minimal_change_check(与 _permission_check 同级):self._mc_guard 惰性实例化(每会话一个),异常 fail-open 返回 None;__init__ 加 self._mc_guard = None
4. AgentLoopConfig.intel 不动

cli/assemble.py L69-80:同步 _asyncio.run(ensure_indexed()) 300s 阻塞 → discoverable 则 start_background_index()(后台线程不阻塞),否则 intel_service=None

cli/__init__.py L211:enable_intel=True → os.environ.get("CODESAGE_NO_INTEL") != "1"(实现缺失开关)

intel/__init__.py:删除 minimal_change_guard/ladder_suggestion 导出(单测同步更新)

intel/demo.py:对齐新 API(guard 返回 ToolResult 取 .content、impact 归一化按 status 分支展示),保持 python -m codesage.intel.demo 可跑

5. 测试(4 文件,镜像 spec 20 §7)

┌───────────────────────────────────────────────────────┬─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
│                         文件                          │                                                                                要点                                                                                 │
├───────────────────────────────────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ tests/intel/test_service.py(重写)                     │ FakeCaller 双路径:参数格式(JSON 对象非 --flag)/parse_structured/parse_table/服务端错误降级/ambiguous 透传/文件目标聚合/ensure_indexed 幂等/后台索引                 │
│                                                       │ wait_ready/结果缓存/超时降级;真实 cbm skipif 保留 + 新增「短名 run → ambiguous」锁定修正行为                                                                        │
├───────────────────────────────────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ tests/intel/test_minimal_change.py(重写)              │ FakeIntel 脚本化:ambiguous 建议含候选/0 调用者 YAGNI/1 调用者根因/N 调用者共享函数+前 3/重复同目标第二次放行/异常 fail-open/建议含 add when                         │
│                                                       │ 与重试放行说明/minimal_change_blocked error_code                                                                                                                    │
├───────────────────────────────────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ tests/intel/test_ponytail.py(重写)                    │ 6 技能全注册(PONYTAIL_FULL_BODY 含 7 档阶梯/Persistence/Intensity 表/示例/When NOT/Boundaries/输出契约)/ponytail_body_for 三模式过滤断言(含统一头)/状态机           │
│                                                       │ env>flag>full 优先级/flag 读写往返/off 删 flag/is_off_phrase 整条匹配/注册幂等/命令注册命中                                                                         │
├───────────────────────────────────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ tests/engine/test_minimal_change_integration.py(扩展) │ 队列级:minimal_change_check 拦 Write → 工具未执行 + sibling Read 不受株连/重复放行 → 工具执行;loop 级:AgentLoopConfig(intel=FakeIntel) 首次 Edit 被拦 +             │
│                                                       │ _apply_minimal_change_advice 不存在;权限决策链零改动回归                                                                                                            │
└───────────────────────────────────────────────────────┴─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘

6. 实施步骤(依赖排序,每步可独立提交)

┌─────┬───────────────────────────────────────────────────────────────────────────┬────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
│ 步  │                                   内容                                    │                                                            闸门                                                            │
├─────┼───────────────────────────────────────────────────────────────────────────┼────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ S1  │ service.py 重写                                                           │ pytest tests/intel/test_service.py 绿(含真实 cbm);python -m codesage.intel.demo 打印结构化 trace                           │
├─────┼───────────────────────────────────────────────────────────────────────────┼────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ S2  │ minimal_change.py 重写 + intel/__init__.py 导出更新                       │ pytest tests/intel/test_minimal_change.py 绿                                                                               │
├─────┼───────────────────────────────────────────────────────────────────────────┼────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ S3  │ ponytail.py 重写(全文+状态机+过滤+6 技能+命令+注入)                       │ pytest tests/intel/test_ponytail.py 绿;三模式输出 diff 目检                                                                │
├─────┼───────────────────────────────────────────────────────────────────────────┼────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ S4  │ 接线:tool_queue.py + loop.py                                              │ pytest tests/engine tests/tools -x 回归绿                                                                                  │
├─────┼───────────────────────────────────────────────────────────────────────────┼────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ S5  │ 装配:assemble.py + cli/init.py + commands.py + repl.py + agents/runner.py │ pytest tests/cli tests/agents -x 绿;手工冒烟:启动无 300s 阻塞、首个 Edit 拦截建议、/ponytail lite 生效、stop ponytail 停用 │
├─────┼───────────────────────────────────────────────────────────────────────────┼────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ S6  │ demo.py 刷新 + 全量回归                                                   │ pytest -x -q 全绿;端到端:索引→影响面→最小建议                                                                              │
└─────┴───────────────────────────────────────────────────────────────────────────┴────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘

7. 风险与缓解(关键几条)

- R1 cbm 输出形状漂移 → 统一解包收敛一处 + 防御式取值 + 真实 cbm 测试锁行为
- R2 只用已确认的 trace_path format=json + ambiguous 过滤,不赌 search_graph/query_graph 形状(ponytail: 注释天花板,seam 留升级口)
- R3 首次写操作撞后台索引未完成 → wait_ready 限时 10s fail-open 放行
- R4 拦一次被模型滥用 → 拦截信息即价值,logger 留痕,覆盖协议显式告知
- R5 删除旧导出破坏导入 → 与测试同 commit 更新,全量回归兜底

关键文件

- 重写:codesage/codesage/intel/service.py、minimal_change.py、ponytail.py
- 接线:engine/tool_queue.py、engine/loop.py、cli/assemble.py、cli/__init__.py、cli/commands.py、cli/repl.py、agents/runner.py、intel/__init__.py、intel/demo.py
- 测试:4 个 tests 文件(路径见上)
- 参考(只读):ponytail/skills/ponytail/SKILL.md 等 6 个 SKILL.md 原文

验证

1. python -m pytest tests/ -q 全绿(含真实 cbm 路径,未装时 skipif)
2. python -m codesage.intel.demo 端到端:索引 → 影响面 → 最小建议
3. 手工冒烟:启动无阻塞、首个 Edit 写前拦截建议、重试放行、/ponytail lite 切换、stop ponytail 停用、子代理注入
4. 价值对比(可选):同一任务有无约束层 diff 行数对比(spec 20 §6.2)
---

# V2 插件化转向(阶段 21-25,2026-08-22 决策)

## 1. 决策

1. **语言不变**:Python ≥3.11 + asyncio,继续零新依赖(纯 stdlib 内核)
2. **自研内核,不引 cordis-py**:Cordis(TS)2709 行无法直接复用;cordis-py 是第三方移植版(新依赖 + API 绑定 + 排错成本);cordis-mini(600 行纯 Python)已验证五概念可完整落地 —— 自研约 1200~1900 行,正好一个阶段
3. **改造 = 加组合层,非重写**:13 模块契约层不动,每模块一个插件文件 + manifest 装配;总计内核 1200~1900 + 改造 2300~3700 行,5 阶段(21-25)
4. **深度档位**:表面插件化(基线)+ 会话事件溯源派生层(采纳);waterfall/claim 后置;能力对齐档(sandbox/web/workflow)明确不做,按业务需求另排
5. **兼容**:`cli/assemble.py` 转 compat shim 全程保留,阶段 25 才删 —— 既有 21K 行测试几乎不动

## 2. 为什么现在转向

- 阶段 01-20 已建成完整 harness(权限/审计/子代理/技能/MCP/intel),但**组合硬编码**:换模型/换工具/换策略要改 Python 代码
- DSH 深度解读(第一章-第五章)确认:插件化的价值在**配置级组装 + 卸载回滚 + 接缝解耦**,CodeSage 已有契约层 + 组合根(assemble.py = 构造器 DI),缺的只是组合层 —— 增量小而价值大
- 学习目标本身:五概念(插件/上下文/inject/事件/effect)是 harness 架构的核心范式

## 3. 关键参照

- DSH 文档(只读):`deepseek-harness/docs/{architecture,cordis-primer,capability-seams,event-producer-consumer}.md`
- cordis-mini(思路参考,勿抄代码):五概念 + Fake LLM 最小 loop
- 本仓库:`docs/specs/21-plugin-kernel.md`(内核规格)、`docs/specs/22-pluginization.md`(改造路线图)

## 4. 与原路线图的关系

- 阶段 19(plugins 热插拔注册层)由 21+ 取代,验收标准(≥2 实现切换零侵入)由 23+ 接棒
- 阶段 16/17/18(bash-safety/memory/multimodel)未交付,保持原编号,可穿插在 V2 各阶段之间按需推进(依赖互不阻塞,见 plan.md)
